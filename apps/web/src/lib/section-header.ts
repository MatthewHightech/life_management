/** Shared card shell for module section blocks. */
export const sectionCardClass = "overflow-hidden rounded-xl border border-border-subtle bg-surface";

/** Shared strip header for module section cards (meals, gear, receipts, etc.). */
export const sectionHeaderClass = "border-b border-border-subtle bg-background px-4 py-3";
