import { ModuleRouteShell } from "@/components/shell/module-route-shell";

export default function MealsLayout({ children }: { children: React.ReactNode }) {
  return <ModuleRouteShell>{children}</ModuleRouteShell>;
}
